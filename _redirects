# Cloudflare Pages Redirects
# Ensure API functions take precedence over the SPA fallback
/api/*      /api/*        200

# Service Worker must be served from root scope
/sw.js       /sw.js        200

# SPA fallback
/*           /index.html   200
